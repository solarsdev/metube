mixin videoBlock(video = {})
  a.video(href=routes.videoDetail(video.id)) 
    video.video__thumbnail(src=video.fileUrl, controls=true)
    span.video__title= video.title
    span.video__views= video.views 