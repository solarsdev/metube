extends layouts/main 

block content 
  .join 
    .join__header 
      h1 アカウント新規登録
    .join__progress.progress
      .progress__step1 
        span 新規登録
      .progress__step2 
        span メールアドレスの確認
      .progress__step3 
        span 登録完了
    .join__login 
      span すでにアカウントをお持ちの方はこちら
      i.fas.fa-chevron-right
    .join__social.social-join
      .social-join__header 
        span 外部サービスで新規登録
      .social-join__btn.social-join__btn--facebook
        i.fab.fa-facebook
        span.social-join__desc.social-join__desc--facebook Facebookで登録
      .social-join__btn.social-join__btn--twitter
        i.fab.fa-twitter
        span.social-join__desc.social-join__desc--twitter Twitterで登録
      .social-join__btn.social-join__btn--google
        i.fab.fa-google
        span.social-join__desc.social-join__desc--google Googleで登録
      .social-join__footer 
        span Facebook、Twitter、Googleアカウントを使い#{siteName}にログインできます。
        span ログイン以外の目的に使われることはありません。
        span #{siteName}利用規約をお読みになり同意の上、登録へお進みください。
    .join__email.email-join
      .email-join__header 
        span メールアドレスで新規登録
      form.email-join__form(action=routes.join, method="post") 
        .email-join__name
          input.email-join__last-name(type="text", name="lastName", placeholder="姓")
          input.email-join__first-name(type="text", name="firstName", placeholder="名")
        input.email-join__email(type="email", name="email", placeholder="メールアドレス")
        input.email-join__password(type="password", name="password", placeholder="パスワード")
        input.email-join__password-confirm(type="password", name="passwordConfirm", placeholder="パスワード（確認）")
        span ご登録前に#{siteName}利用規約をお読みください。
        .email-join__agree
          input.email-join__agree#email-join__agree(type="checkbox", name="agree")
          label(for="email-join__agree") #{siteName}利用規約に同意する
        input.email-join__submit(type="submit", value="登録する")
